import React from "react";
import { Container } from "@mui/material";
import DataTable from "../../components/dataTable";

const Position = () => {
	return (
		<Container>
			<DataTable title="Position Table" headers={[]} data={[]} />
		</Container>
	);
};
export default Position;